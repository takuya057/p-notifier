# 管理サイトの全Cookieヘッダー値
# Chrome DevTools → Application → Cookies → tool.gacha-station.com の全Cookieを
# `名前=値; 名前=値; 名前=値` 形式で記述（XSRF-TOKEN, laravel_session, ランダム名のセッションキー）
GACHA_COOKIE=

# Discord Webhook URL
DISCORD_WEBHOOK_URL=
